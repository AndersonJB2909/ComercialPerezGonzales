<Application x:Class="ComercialPerezGonzales.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:ComercialPerezGonzales.Helpers.Converters"
             xmlns:vm="clr-namespace:ComercialPerezGonzales.ViewModels.POS"
             xmlns:vmi="clr-namespace:ComercialPerezGonzales.ViewModels.Inventario"
             xmlns:vmc="clr-namespace:ComercialPerezGonzales.ViewModels.Clientes"
             xmlns:vmr="clr-namespace:ComercialPerezGonzales.ViewModels.Reportes"
             xmlns:vmt="clr-namespace:ComercialPerezGonzales.ViewModels.Tablero"
             xmlns:views="clr-namespace:ComercialPerezGonzales.Views.POS"
             xmlns:viewst="clr-namespace:ComercialPerezGonzales.Views.Tablero"
             xmlns:viewsi="clr-namespace:ComercialPerezGonzales.Views.Inventario"
             xmlns:viewsc="clr-namespace:ComercialPerezGonzales.Views.Clientes"
             xmlns:viewsr="clr-namespace:ComercialPerezGonzales.Views.Reportes"
             xmlns:vmconf="clr-namespace:ComercialPerezGonzales.ViewModels.Configuracion"
             xmlns:viewsconf="clr-namespace:ComercialPerezGonzales.Views.Configuracion"
             xmlns:vmcd="clr-namespace:ComercialPerezGonzales.ViewModels.CierreDia"
             xmlns:viewscd="clr-namespace:ComercialPerezGonzales.Views.CierreDia">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Dark.Blue.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Tamaño de fuente base para toda la app -->
            <Style TargetType="TextBlock">
                <Setter Property="FontSize" Value="14"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="FontSize" Value="14"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="FontSize" Value="14"/>
            </Style>
            <Style TargetType="DataGrid">
                <Setter Property="FontSize" Value="14"/>
            </Style>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="FontSize" Value="14"/>
            </Style>
            <Style TargetType="ComboBox">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="#152238"/>
                <Setter Property="BorderBrush" Value="#1A3A2A"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <!-- Caja cerrada -->
                                <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="2">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="30"/>
                                        </Grid.ColumnDefinitions>
                                        <ContentPresenter Grid.Column="0"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                          Margin="{TemplateBinding Padding}"
                                                          VerticalAlignment="Center"
                                                          IsHitTestVisible="False"/>
                                        <!-- Flecha -->
                                        <Path Grid.Column="1"
                                              Data="M0,0 L6,6 L12,0"
                                              Stroke="#22C55E" StrokeThickness="2"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"
                                              IsHitTestVisible="False"/>
                                        <!-- Zona clickeable invisible -->
                                        <ToggleButton Grid.ColumnSpan="2"
                                                      Background="Transparent" BorderThickness="0"
                                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                      Focusable="False" ClickMode="Press"/>
                                    </Grid>
                                </Border>
                                <!-- Popup desplegable -->
                                <Popup x:Name="Popup"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Slide"
                                       Placement="Bottom">
                                    <Grid MinWidth="{TemplateBinding ActualWidth}"
                                          MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border Background="#152238"
                                                BorderBrush="#22C55E"
                                                BorderThickness="1"
                                                CornerRadius="0,0,2,2">
                                            <ScrollViewer CanContentScroll="True"
                                                          VerticalScrollBarVisibility="Auto">
                                                <ItemsPresenter/>
                                            </ScrollViewer>
                                        </Border>
                                    </Grid>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#22C55E"/>
                                </Trigger>
                                <Trigger Property="IsDropDownOpen" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#22C55E"/>
                                    <Setter TargetName="Border" Property="CornerRadius" Value="2,2,0,0"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Background" Value="#152238"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#1A3A2A"/>
                                    <Setter Property="Foreground" Value="#22C55E"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#14532D"/>
                                    <Setter Property="Foreground" Value="#4ADE80"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="DatePicker">
                <Setter Property="FontSize" Value="14"/>
            </Style>

            <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
            <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility" />
            <converters:CountToVisibilityConverter x:Key="CountToVisibility" />
            <converters:CountToInverseVisibilityConverter x:Key="CountToInverseVisibility" />
            <converters:CurrencyConverter x:Key="CurrencyConverter" />
            <converters:StringToVisibilityConverter x:Key="StringToVisibility" />
            <converters:StringToInverseVisibilityConverter x:Key="StringToInverseVisibility" />
            <converters:ByteArrayToImageConverter x:Key="BytesToImage" />
            <converters:ByteArrayToVisibilityConverter x:Key="BytesToVisibility" />
            <converters:ByteArrayToInverseVisibilityConverter x:Key="BytesToInverseVisibility" />

            <DataTemplate DataType="{x:Type vmt:TableroViewModel}">
                <viewst:TableroView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:PosViewModel}">
                <views:PosView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vmi:ProductosViewModel}">
                <viewsi:ProductosView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vmc:ClientesViewModel}">
                <viewsc:ClientesView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vmr:ReportesViewModel}">
                <viewsr:ReportesView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vmconf:ConfiguracionImpresionViewModel}">
                <viewsconf:ConfiguracionImpresionView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vmcd:CierreDiaViewModel}">
                <viewscd:CierreDiaView />
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
